import { createNextConfig } from '@mappnext/next-config';

export default createNextConfig({
  nextConfig: {
    // Your app-specific configuration
  },
  additionalRemotePatterns: [
    // Your app-specific remote patterns
  ],
  additionalTranspilePackages: [
    // Your app-specific packages to transpile
  ],
});
